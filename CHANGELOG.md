# Changelog

## 2026-08-25 (밤)

### 탐지 라벨 확장 (cat|dog|person)

- **미탐 원인 실증** — 모델이 흰 고양이의 "앉은 뒷모습"을 dog 0.727로 분류
  (bbox가 실사진의 고양이 위치와 일치, 원시 출력에 cat 0회). cat 전용 필터가
  동물 탐지를 버리고 있었다.
- **hailo-camera**: watchcat-cat 전용 `config/yolov8s_nms.json`의 allowed_labels를
  `['cat','dog','person']`으로 확장 (`af23616`, 골프는 yolov8s_nms_golf.json이라 무영향).
- **게이트웨이**: cat|dog → `catPresent`(개 없는 집이므로 dog=오월이),
  person → 별도 `personPresent`/`personBoxes` (사진 위 파란 점선 박스, DETAIL에
  PERSON 행). 배포 후 놓쳤던 실사진 재추론으로 dog×7·person×7 통과 확인. (`a9bc71a`)

### 레이더 움직임 이력

- **움직임 이벤트 기록** — 게이트웨이가 관측 흐름을 서버측 최근접 매칭으로
  "움직임 에피소드"로 잘라 기록: 250mm 이상 실이동만 점으로 채택(정지 반사체의
  지터가 몇 시간짜리 이벤트로 늘어지지 않게), 3초 소실 또는 8초 정지 시 종료,
  경로 600mm 미만은 폐기. 이벤트당 시각·지속·경로 폴리라인·이동거리·최고속도.
- **보관기간 제한** — 일자별 JSONL(`uploads/watchcat/radar-events/`)로 저장,
  기본 7일 지나면 자동 삭제 (`WATCHCAT_RADAR_EVENT_RETENTION_DAYS`). 메모리에는
  최근 200건, 재시작 시 디스크에서 복원.
- **웹 표시** — `GET /api/v1/radar/events`, 레이더 페이지 하단 "최근 움직임" 목록
  (부채꼴 미니 썸네일 + 한 줄 요약, 탭하면 본 지도에 6초 유령 궤적 재생),
  허브 레이더 카드에 "오늘 움직임 N건". 테스트 6건 추가.

## 2026-08-21 (오후·밤) — 레이더 Phase R1

### 게이트웨이 + 웹

- **레이더 수신 API** — `POST /api/v1/radar/observations` (센서 허용 목록, 물리 범위
  검증, 30초 메모리 링 버퍼) + `GET /api/v1/radar/status`. 이미지 상태와 분리된
  별도 모듈 `gateway/radar.js`. 테스트 포함. (`0795182`)
- **부채꼴 실시간 지도** — https://radar.linkus-plz.com 신설 (Cloudflare Tunnel
  ingress + DNS 추가). ±60° 시야, 1m 거리 링, 2/4/8m 줌, 10초 잔상 궤적,
  0.5초 폴링. `/radar` 경로로도 접근 가능. (`764b422`)
- **몸집 버블** — 타겟을 폴링 간 최근접 매칭으로 추적해, 3초 창의 직선 경로 대비
  RMS 흔들림을 원으로 표시 (±cm 라벨). 실측 결과 사람 보행 중앙값 ±2.5cm로,
  레이더 내부 스무딩 때문에 **몸집 판별 신호로는 부족** — 시각화로만 유지.
  (`0646126`, 배율 실험 후 실측 크기로 확정)
- **기존 메인 페이지에 레이더 요약 줄 추가.** (`0795182`)

### 레이더 노드 (XIAO ESP32C3)

- **radar_node 펌웨어** — R0에서 실측 확정한 프레임 파서 + 비블로킹 Wi-Fi 재연결 +
  keep-alive TLS로 최신 배치만 4Hz 전송 (오래된 배치 재전송 금지). (`b030525`)
- **하트비트** — 레이더 프레임이 없어도 2초마다 `radarOk:false` 빈 배치를 보내,
  "노드 죽음"(빨강)과 "레이더 무신호"(주황)를 원격에서 구분 가능하게. 이 구분이
  없어서 하루 종일 헤맨 날의 산물.

### 발견·교훈 (docs/ld2454-r0-observations.md에 상세)

- **R(UART_RX) 핀이 부팅을 막을 수 있다** — R이 부팅 순간 잘못된 레벨이면 모듈이
  기동하지 않아 죽은 것처럼 보인다 (모듈 2개가 동반 불량으로 위장했던 원인).
  설정 명령을 쓸 일이 없으면 R은 미연결이 기본.
- 8m는 설정이 아니라 최대 성능치 — LD2450 계열 프로토콜에 거리 설정 명령 자체가
  없음 (공식 프로토콜 문서 전문 확인).
- 보조배터리는 저전류(~100mA) 자동 차단으로 노드를 끊는다 — 이동 테스트는
  저전류 모드나 벽 어댑터로.
- `radar_probe`에 RX 선 레벨 진단 추가 — 침묵하는 모듈과 끊긴 선을 구분.

## 2026-08-21

### 캣센서 (XIAO ESP32S3 + OV3660)

- **AEC 게인 상한 버그 수정** — 드라이버 `set_gainceiling()`이 enum 서수를 1/16 게인
  단위 레지스터(0x3A18/19)에 그대로 써서 상한이 1x 미만에 걸려 있었다. 센서 기본값
  15.5x를 직접 기록해 어두운 프레임 문제 해소. (`182d0c4`)
- **노출 정상화** — 게인이 살아나자 어두운 시절의 보상(ae_level 1, brightness 2)이
  과노출(평균 192/255)로 이어져 둘 다 중립으로 되돌림. 실측 휘도 160대. (`182d0c4`)
- **정지촬영 실패 수정** — jpeg_quality 2는 밝은 장면에서 드라이버의 고정 프레임 버퍼
  (UXGA 384KB, 품질 무관)를 초과해 모든 캡처가 실패했다. 4로 복원. (`182d0c4`)
- 위 수정으로 **흰 고양이 첫 정상 탐지** (conf 0.52~0.58) — 기존 chair/bed 오분류의
  원인이 저조도 화질이었음을 확인.

### 캣모니터 (ESP32-S3 N16R8 + ST7789)

- **B2 즉시 피드백** — 캡처 요청 전에 "Requesting..."을 먼저 그려 TLS 대기 중에도
  눌림이 보이게. (`66e095c`)
- **PSRAM 활성화** — N16R8의 Octal PSRAM 8MB 인식(qio_opi), 사진 상한 320KB→512KB.
  밝아진 사진(313~343KB)이 옛 상한을 넘던 문제 해소. (`66e095c`)
- **탐지 박스 오버레이** — PHOTO 페이지 사진 위에 추론이 찾은 고양이 위치를 초록
  사각형으로 표시. (`9a81557`)
- **상태 폴링 구조 개선** — 전용 태스크 + keep-alive TLS 세션으로 이전. 폴링마다
  하던 TLS 핸드셰이크가 사라져 버튼 반응이 즉각적으로. 캡처 직후 in-flight 폴 결과는
  한 사이클 폐기해 화면 되돌아감 방지. (`9a81557`)
- **색상 정리** — CAT FOUND 초록, NO CAT 흰색, ERROR 빨강. (`9a81557`)
- **레이아웃 수정** — 사진 캡션과 페이지 네비게이션 겹침 해소 (사진 y64, 캡션 y218).
  (`c5229a0`)

### 게이트웨이 (Pi)

- **status에 `catBoxes` 추가** — 정규화 [x,y,w,h] 최대 4개, 캡처 큐잉 시 초기화.
  (`56008fd`)
- **배포 방식 전환** — Pi의 수동 복사본(구버전)을 저장소 git clone으로 교체.
  이후 배포는 push → Pi에서 `git pull` → 서비스 재시작. 구버전에 없던 명령 재전달
  상한(무한 재전달로 인한 "Waiting for the sensor" 영구 고착 원인)과 latestFilename
  라이브 덮임 수정이 함께 반영됨.

### LD2454 레이더 (Phase R0, 미커밋)

- XIAO ESP32C3 + `radar_probe_c3` env(수신 전용 관찰 도구) 추가.
- 프레임 형식 실측 확정 — LD2450 호환 30바이트 프레임, 16Hz, mm/cm/s 단위,
  MSB=양수 부호-크기. 상세: `docs/ld2454-r0-observations.md`.
- 배선 함정 기록: 동봉 케이블 색 반전(검정=5V, 빨강=GND), 안테나 면 방향.
- 다음 단계: Phase R1 (radar_node 펌웨어 + 게이트웨이 수신 API).
